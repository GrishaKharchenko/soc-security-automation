# --- runtime -------------------------------------------------------------
requests==2.32.3          # HTTP-клиент для VirusTotal API v3
python-dotenv==1.0.1      # загрузка конфигурации из .env

# --- dev / test ----------------------------------------------------------
pytest==8.3.3             # тестовый фреймворк
requests-mock==1.12.1     # мок HTTP-слоя, тесты не ходят в реальный VT
