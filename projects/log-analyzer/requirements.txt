# --- runtime -------------------------------------------------------------
python-dotenv==1.0.1      # конфигурация из .env
# Всё остальное — стандартная библиотека: re, csv, json, datetime,
# collections, ipaddress. Для разбора логов внешние пакеты не нужны,
# а меньше зависимостей = меньше поверхность атаки и проще развёртывание.

# --- dev / test ----------------------------------------------------------
pytest==8.3.3
