# Оболочка не тянет зависимости отдельных модулей напрямую: она их импортирует
# и наследует. Здесь только то, что нужно ей самой.
python-dotenv==1.0.1
requests==2.32.3          # через ioc-analyzer

# --- dev / test ----------------------------------------------------------
pytest==8.3.3
requests-mock==1.12.1
